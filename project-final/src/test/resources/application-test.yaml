spring.cache.type: none
spring:
  sql:
    init:
      mode: never
  datasource:
    # Use Testcontainers JDBC URL for ephemeral PostgreSQL during tests. No local DB needed.
    url: jdbc:tc:postgresql:17:///jira-test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: jira
    password: CodeGymJira